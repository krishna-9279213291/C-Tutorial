// in this example , we hav an it variable age and a float variable percent. The printf() function printd the values of noth as below -

#include <stdio.h>
int main (){
    int age = 18; float percent = 67.75;
    printf("Age : %d \nPercent : %f" , age , percent);
    return 0;
}

//The value of the first variable replaces the first format specifier %d. Similarly, %f is substituted by the value of the percent variable.
