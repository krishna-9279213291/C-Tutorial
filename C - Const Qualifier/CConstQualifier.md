C - Const Qualifier : 

The const qualifier in c is used to declare varibles whose values should remain fixed throughout the progra. it is used to declare a variable as a constant , whose value cannot be chnaged after intialization. Once a variable is declared as const, it becomes read- only   , and the compiler prevents any accidental mmodifications. 

what is the use of const qualifier in c? :

using the const qualifier, programmers can improve the safety, readability, and maintainability of their program codes. it helps programmers make their intentions clear 
by signaling which values should stay 